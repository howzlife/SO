%nav.pager
	- if @purchase_orders.first_page?
		%span <
	- else
		= link_to_previous_page @purchase_orders, '<'
	.vdivider
	- if @purchase_orders.last_page?
		%span >
	- else
		= link_to_next_page @purchase_orders, '>'
